// Dashboard charts utility helpers
// Chart.js global configurations can be set here
if (window.Chart) {
    Chart.defaults.font.family = "'Outfit', sans-serif";
    Chart.defaults.color = 'rgba(148, 163, 184, 1)'; // muted text color
}
