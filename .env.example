# Flask Config
FLASK_APP=run.py
FLASK_ENV=development
SECRET_KEY=replace-with-a-secure-secret-key-in-production

# MySQL Database Config (Leave empty to use SQLite fallback)
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
DB_NAME=smart_transport
